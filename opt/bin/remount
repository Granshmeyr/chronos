#!/bin/bash
#This script is a companion to /etc/init/remount.conf, which remounts every mount point with exec

MOUNTS=$( cat /etc/mtab | cut -d ' ' -f2 )
for line in $MOUNTS
do
        sudo mount -o remount,rw,exec,symfollow "$line"
done
