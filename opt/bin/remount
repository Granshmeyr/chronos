#!/bin/bash
# This script is a companion to /etc/init/remount.conf; removes mount flags post login

MOUNTS=$( cat /etc/mtab | cut -d ' ' -f2 )
for line in $MOUNTS
do
        sudo mount -o remount,rw,exec,suid,dev,symfollow "$line"
done
